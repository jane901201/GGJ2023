using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SelfSceneObject : BaseSceneObject
{
    public override SceneObjectType ObjectType { get => SceneObjectType.Self; }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
